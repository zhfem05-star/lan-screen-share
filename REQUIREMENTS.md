# LAN 화면 스트리밍 프로젝트 요구사항 및 실행 계획

최종 업데이트: 2026-08-07

## 1. 프로젝트 목적

- 같은 LAN 내에서 PC 화면(+오디오)을 캡처해 iPad로 실시간 송출/수신
- 기존 디스코드 화면공유(통화 서버 경유)의 인풋렉 문제를 해결하기 위한 대안
- 목표: "속도 차이 없이 빠르게" 화면을 보는 것 (게임 스트리밍 수준의 극한 반응속도는 불필요)
- 개인 학습/포트폴리오 목적의 직접 구현 프로젝트

## 2. 핵심 요구사항

- 같은 LAN 환경에서 동작 (외부 릴레이 서버 경유 없음, 포트포워딩 불필요 — LAN 내부에서만 서로 직접 도달 가능)
- 지연을 최소화하는 P2P 전송 구조
- 화면 + 오디오를 iPad에서 수신 (입력 제어는 불필요 — 순수 뷰어. 입력은 별도 무선 기기로 PC를 직접 조종)
- 정량 목표: 지연 20ms 내외, 최대 30ms까지 허용

## 3. 검토했던 대안

| 방식 | 특징 | 채택 여부 |
|---|---|---|
| Discord 화면공유 | 통화 서버 경유, 다수 네트워크 홉 → 인풋렉 | 기각 (이 문제 때문에 프로젝트 시작) |
| Sunshine + Moonlight | 오픈소스 self-hosted 스트리밍 서버/클라이언트, NVENC 하드웨어 인코딩, LAN 실측 10~20ms | 참고용 (직접 구현이 목적이라 채택 안 함, 필요 시 즉시 대안으로 사용 가능) |
| WebRTC 커스텀 구현 | 시그널링/미디어 분리 구조, LAN 내 P2P 전송, 확장성과 학습 가치 높음 | **채택** |

## 4. 아키텍처 결정

### 4.1 전체 구조

```
[PC: 화면+오디오 캡처 + 인코딩] → (시그널링 서버 경유 최초 핸드셰이크, WebSocket 상시 연결) → [PC ↔ iPad 직접 P2P UDP/SRTP 스트림]
```

- 시그널링 서버: SDP/ICE candidate 교환 + 재협상(ICE restart)용. 최초 핸드셰이크 이후에도 **WebSocket 연결을 계속 유지**해야 함 (일회성 HTTP 요청/응답 방식은 재연결 로직 구현이 불가능하므로 사용 안 함)
- 실제 영상/오디오 데이터: 연결 수립 후 시그널링 서버를 거치지 않고 PC ↔ iPad 간 직접 P2P 전송
- 데이터채널: 불필요 (입력 제어 없음, 미디어 트랙만 사용)

### 4.2 1단계 (현재 채택): 브라우저 기반 캡처

- PC: Node.js 경량 웹서버 실행 → 시그널링 서버 겸 페이지 호스팅
- PC 크롬에서 `getDisplayMedia({ video: true, audio: true })`로 화면+오디오 캡처 → WebRTC로 스트림 송출
- iPad: Safari로 PC의 LAN IP(예: `192.168.0.x:포트`) 접속 → PIN 인증 → `<video>` 태그로 스트림 수신
- 장점: 구현 속도 빠름, 시그널링 로직을 이후 단계에서도 재사용 가능
- 단점: 브라우저 캡처 파이프라인 특성상 네이티브 캡처 대비 프레임당 약 20~40ms 추가 지연 예상
  - **리스크**: Sunshine/Moonlight 네이티브 캡처 참고치가 10~20ms인 점을 감안하면, 이 단계만으로 20~30ms 목표 달성이 빠듯할 수 있음. 1단계 실측 결과가 30ms를 넘으면 "필요 시"가 아니라 **바로 2단계 착수를 기본 시나리오로 설정**

### 4.3 2단계 (조건부 확장): 네이티브 캡처로 전환

- 발동 조건: 1단계 실측 지연이 30ms를 초과할 경우
- Windows DXGI Desktop Duplication API로 GPU 프레임버퍼 직접 캡처
- 하드웨어 인코더(NVENC/QuickSync)로 H.264 인코딩, `zerolatency` 프리셋 + 짧은 GOP
- libdatachannel 등으로 WebRTC 미디어 트랙 직접 생성
- iPad 수신은 네이티브 앱(Swift + WebRTC 클라이언트 라이브러리) 또는 최소 WKWebView로 전환 가능
- 시그널링 서버 코드는 1단계에서 그대로 재사용

## 5. 세부 기술 결정

### 5.1 코덱 — H.264 고정
- LAN 환경이라 대역폭 여유 있음 → 압축 효율보다 호환성/하드웨어 가속 우선
- iPad Safari 하드웨어 디코드 확실히 지원, Chrome도 하드웨어 인코드 활용 가능
- `RTCRtpSender.setCodecPreferences()`로 H.264를 최우선 코덱으로 명시적으로 강제
- VP9/AV1은 압축률은 좋으나 Safari 지원 불안정 + 소프트웨어 인코딩 부하로 지연 증가 위험 → 채택 안 함

### 5.2 오디오 — 믹싱 미지원, 단순 재생만
- `getDisplayMedia({ audio: true })`로 시스템 오디오 캡처해 함께 송출
- iOS Safari는 웹페이지의 오디오 세션 카테고리(`AVAudioSession`)를 JS로 제어할 방법이 없어, 다른 앱(예: 디스코드) 오디오와의 믹싱을 보장할 수 없음
- **결정: 믹싱 지원 포기.** 이 프로젝트 오디오 재생 시 다른 앱 오디오가 끊기거나 반대로 막힐 수 있음을 감수. 필요하면 사용자가 수동으로 다른 앱을 음소거하고 사용
- mDNS ICE candidate 이슈(Chrome이 로컬 IP 대신 `*.local` 이름을 candidate로 사용, Safari에서 resolve 실패 가능)는 사전 대응 없이 **실제 구현 중 부딪히면 그때 대응**

### 5.3 접근 제어 — PIN 인증
- 포트포워딩은 불필요 (LAN 내부 직접 통신이므로 해당 없음)
- 시그널링 서버 페이지 접속 시 **PIN/페어링 코드 인증** 적용 (예: PC 화면에 4자리 코드 표시 → iPad 접속 시 입력)
- LAN 내 임의의 사용자가 IP:포트만 알고 무단 접속하는 것을 막기 위한 최소한의 장치

### 5.4 재연결 정책
- 시그널링 서버와의 WebSocket 연결을 상시 유지 (재협상을 위해 필요)
- `connectionState === "disconnected"` (일시적 문제) → 몇 초간 브라우저 자체 ICE 복구 대기
- `connectionState === "failed"` (복구 불가) → 시그널링을 통해 ICE restart 1회 시도
- 재시도도 실패하면 iPad 화면에 alert만 표시 (자동 재시도 무한 반복하지 않음)

## 6. 스코프 밖 (명시적 제외)

- 입력(마우스/키보드) 원격 제어 — 별도 무선 기기로 PC를 직접 조종하는 방식 사용
- 다중 뷰어(1:N) 동시 접속 — 1:1 연결만 고려
- 외부 인터넷 경유 접속 (LAN 밖에서의 원격 접속)
- 다중 모니터 선택 UI — 1단계에서는 생략, 필요 시 추후 추가

---

실행 단계(구체적 To-do, Phase 0~6)는 별도 파일 [ROADMAP.md](ROADMAP.md) 참고.
